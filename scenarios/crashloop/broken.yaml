apiVersion: v1
kind: Pod
metadata:
  name: crashloop-demo
  namespace: sre-lab
spec:
  containers:
    - name: app
      image: busybox:1.36
      command: ["sh", "-c", "echo starting; sleep 2; echo fatal startup error >&2; exit 1"]
